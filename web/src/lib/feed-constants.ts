/** Page size for home feed + `/api/feed/user-needs` (client-safe). */
export const FEED_PAGE_SIZE = 12;

/** Hard cap per request for `/api/feed/user-needs`. */
export const FEED_PAGE_MAX = 50;

/** Reel-style feed: each slide matches this height (100dvh minus header + spacing). */
export const FEED_REEL_VIEWPORT_CSS = "calc(100dvh - 7.5rem)";
